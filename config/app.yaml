log_level: "INFO"
log_file: "logs/defyvision.log"

service:
  # Bypass temporal del login de Modo Servicio.
  # Volver a true cuando se quiera exigir usuario/contrasena.
  login_enabled: false
  username: "DEFYMOTION"
  password: "defy2026"

camera:
  retry_interval_s: 3.0 # segundos de espera entre cada intento de reconexión
